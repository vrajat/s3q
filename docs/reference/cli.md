# CLI Reference

The CLI is planned to live in Python and call the Python SDK.

Planned command groups:

- `s3q queue`
- `s3q inspect`

Queue commands should mutate queue state. Inspect commands should be read-only.
