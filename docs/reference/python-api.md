# Python API

The Python SDK should be a thin wrapper over the Rust core.

Planned v1 handles:

- `Client`
- `QueueHandle`
- `Producer`
- `Consumer`
- `Inspect`

Python method names should mirror Rust names unless Python syntax requires a small adaptation.
